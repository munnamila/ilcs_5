tmux new -s session1            create a new session
ctrl+b d                        exit from a session
tmux ls                         check session list(out of the session)
ctrl+b s                        check session list(in the session)
tmux a -t session1              access a session
tmux kill-session -t session1   kill a session(out of the session)
control d                       kill a session(in the session)


